{% extends 'base.html.twig' %}

{% block title %}Editor
{% endblock %}

{% block body %}

	<div class="container mt-3">

		<div class="my-3">
			<a href="{{ path('main') }}" class="btn btn-outline-primary">
				Accueil
			</a>
			<a href="{{ path('editor_list') }}" class="btn btn-outline-primary">
				Liste des editeurs
			</a>
		</div>

		<div class="border p-5">
			<h1>{{ editor.name }}</h1>
			<p>{{ editor.country }}</p>
		</div>
	</div>

{% endblock %}
