{% extends 'base.html.twig' %}

{% block title %}Designer
{% endblock %}

{% block body %}

	<div class="container mt-3">

		<div class="my-3">
			<a href="{{ path('main') }}" class="btn btn-outline-primary">
				Accueil
			</a>
			<a href="{{ path('designer_list') }}" class="btn btn-outline-primary">
				Liste des dessinateurs
			</a>
		</div>

		<div class="border p-5">
			<h1>{{ designer.name }}</h1>
			<p>{{ designer.country }}</p>
		</div>
	</div>

{% endblock %}
