{% extends 'base.html.twig' %}

{% block title %}Licence
{% endblock %}

{% block body %}

	<div class="container mt-5">

		<div class="my-3">
			<a href="{{ path('main') }}" class="btn btn-outline-primary">
				Accueil
			</a>
			<a href="{{ path('licence_list') }}" class="btn btn-outline-primary">
				Liste des licences
			</a>
		</div>

		<div class="border p-5">
			<h1>{{ licence.name }}</h1>
			<p>{{ licence.description }}</p>
			<p>{{ licence.media }}</p>
		</div>
	</div>

{% endblock %}
