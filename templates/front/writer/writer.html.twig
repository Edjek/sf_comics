{% extends 'base.html.twig' %}

{% block title %}Writer
{% endblock %}

{% block body %}

	<div class="container mt-5">

		<div class="my-3">
			<a href="{{ path('main') }}" class="btn btn-outline-primary">
				Accueil
			</a>
			<a href="{{ path('writer_list') }}" class="btn btn-outline-primary">
				Liste des auteurs
			</a>
		</div>

		<div class="border p-5">
			<h1>{{ writer.name }}</h1>
			<p>{{ writer.country }}</p>
		</div>
	</div>

{% endblock %}
