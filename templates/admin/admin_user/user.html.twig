{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}

	<div class="container mt-5">
		<div class="border p-5">
			<h1>{{ user.name }} {{ user.firstname }}</h1>
			<p>{{ user.email }}</p>
		</div>
	</div>

{% endblock %}